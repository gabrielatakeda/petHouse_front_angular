export class Usuario { //Define uma classe e ela é usada como molde para representar os dados de um usuário
  //Basicamente como se fossem os atributos do backend
  //O operador ! significa que as propriedades serão inicializadas em algum momento
  nome!: string; 
  email!: string;
  cpf!: string;
  senha!: string;
  usuario!: string;
}